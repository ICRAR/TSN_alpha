.pagination
  Number to display per page:
  %ul
    %li= link_to 10, params.merge(:per_page => 10)
    %li= link_to 20, params.merge(:per_page => 20)
    %li= link_to 50, params.merge(:per_page => 50)