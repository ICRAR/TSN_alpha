<!DOCTYPE html>
<html>
<head>
    <title>theSkyNet - Running...</title>

    <%= stylesheet_link_tag    "application", :media => "all" %>



    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "non_includes/deployJava" %>


    <%= csrf_meta_tags %>
    <script>
        window.addEventListener("beforeunload", function( e ) {
            if (!e) e = window.event;
            e.cancelBubble = true;
            var conf = 'Are you sure you want to leave the page? You will no longer be processing data if you do.';
            e.returnValue = conf;
            if (e.stopPropagation) {
                e.stopPropagation();
                e.preventDefault();
            }
            return conf;
        });
    </script>
</head>
<body data-controller="<%= controller_name %>" data-action="<%= action_name %>" <%=  params[:id] ? "data-id=#{params[:id]}" : "" %> onload="javaCheck()" onbeforeunload="leavingPage()" style="background:#ffffff url(<%= asset_path("images/windowbg.jpg")%>) top center no-repeat;">
<div style="text-align:center;" width="100%" height="100%">
    <%= image_tag("images/logosmall2.png", :style => "margin-top:15px;") %>
    <h3 style="color:#0e6199;" id="contributionMessage">You are now contributing to theSkyNet.</h3>
    <div id="contributionText"><p>Once this window is closed, your contribution will end.</p>
        <% if user_signed_in? %>
            <p>To contribute to the skynet without needing this window open,<br /> why not <strong><a href="#" onClick="window.opener.location.href='download/index';window.opener.focus()">download the background application?</a></strong>.
            </p>
        <% end %>
        <a href="#" onclick="showApplet()" class="button small blue"><span id="appletButton">Show your client</span></a>
        <iframe id="appletFrame" src="http://<%= @server %>/nereus/NereusApplet.html?userID=<%= @nereus_id %>" width="0" height="0" frameBorder="0" scrolling="no" hspace="0" vspace="0" marginheight="0" marginwidth="0"></iframe>
    </div>
    <p id="javaMessage"></p>
</div>
<?php googleAnalytics();?>
</body>
</html>