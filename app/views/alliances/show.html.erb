<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @alliance.name %>
</p>
<p>
  <b>Leader:</b>
  <%= link_to @alliance.leader.name, @alliance.leader %>
</p>

<p>
  <b>Ranking:</b>
  <%= @alliance.ranking %>
</p>
<p>
  <b>Total Members:</b>
  <%= @alliance.members.size %>
</p>
<p>
  <b>Total Credit:</b>
  <%= @alliance.credit %>
</p>

<p>
  <b>Members:</b>
</p>
<table>
  <tr>
    <th>Name</th>
    <th>Credit</th>
    <th>Alliance Rank</th>
    <th>Overall Rank</th>

  </tr>
  <% rank = 1 %>
  <% @members.each do |member| %>
      <tr>
        <td><%= link_to member.name, member %></td>
        <td><%= member.credits %></td>
        <td><%= rank %></td>
        <td><%= member.rank %></td>
        <% rank +=1 %>
      </tr>
  <% end %>
</table>
<div id="alliance_graph"></div>
<%= link_to('Manage |', edit_alliance_path(@alliance)) if  (user_signed_in? && current_user.profile.alliance_leader == @alliance) %>
<%= link_to 'Back', alliances_path %>
