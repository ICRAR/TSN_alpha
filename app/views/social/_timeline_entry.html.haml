.timeline_entry{id: "timeline_entry_#{entry.id}"}
  %p
    =image_tag entry.profile.avatar_url(32), title: entry.profile.name
    =link_to entry.profile.name, entry.profile
    = entry.get_subject
    = " #{distance_of_time_in_words(Time.now, entry.posted_at)} ago."
    - if entry.has_more?
      %button.btn.btn-link{data: {toggle: 'collapse', target: "#timeline_entry_#{entry.id} .collapse"}} More
  - if entry.has_more?
    .collapse.out
      .well.well-small
        %p= entry.get_more
  %hr