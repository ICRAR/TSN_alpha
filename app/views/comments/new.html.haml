- my_meta :title => "New Comment on #{@comment.commentable_name}"
= content_for(:title_bar) do
  #intro-sml-wrap
    %h1.fontface= "New Comment on #{@comment.commentable_name}"


-content_for(:content) do
  #content-wrap
    .well.well-white
      = render 'form', remote: false