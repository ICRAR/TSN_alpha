<p>Enter your POGS Boinc account details to sync your boinc account</p>
<%= form_tag('/profile/update_boinc_id', :method => 'post') do %>
    <%= label_tag(:boinc_user, "Boinc User:") %>
    <%= text_field_tag(:boinc_user) %>    <br/>
    <%= label_tag(:boinc_password, "Boinc Password:") %>
    <%= password_field_tag(:boinc_password) %> <br/>
    <%= submit_tag("update") %>

<% end %>
<br />
<p>Or if you don't have an existing account re-enter your skynet password and we will create a new boinc account for you.</p>
<%= form_tag('/profile/create_boinc_id', :method => 'post') do %>

    <%= label_tag(:password, "Sky Net Password:") %>
    <%= password_field_tag(:password) %> <br/>
    <%= submit_tag("Create Boinc Account") %>
<% end %>